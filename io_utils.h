#include <stdio.h>


/**
 * @brief Clears the input buffer by consuming all the characters until a newline is found
 */
 void clearInputBuffer();

/**
 * @brief Prints the game banner
 */
void printBanner();

/**
 * @brief Clears the screen
 */
void clearScreen();
