#include <stdio.h>

/**
 * @brief Asks the user for input and operates accordingly on the grid
 * @param colorWhoseTurnIsNow The color whose turn is now
 * @return Returns the status code which is 1 if the user wants to exit and otherwise
 */
int handleInputAndGrid(char colorWhoseTurnIsNow);
